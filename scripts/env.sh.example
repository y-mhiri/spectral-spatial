# Copy this file to scripts/env.sh and fill in your machine-specific paths.
# scripts/env.sh is gitignored — never commit it.

export DATASET_PATH="/path/to/dataset.zarr"
export DEVICE="cuda"   # or "cpu"

source /path/to/venv/bin/activate
